---
        import Sidebar from '../components/sidebar.astro';
        import Aplicaciones from '../components/aplicaciones.astro';
        import Layout from '../layouts/Layout.astro';

---

<Layout>
<Sidebar />
<body>
    <!--<div class="preloader" id="preloader">
        <div class="squares-container">
            <div class="square blue"></div>
            <div class="square red"></div>
        </div>
    </div> -->
    <div class="scroll-container" id="scrollContainer">
        <section class="section" data-section="1">
            <div class="section-content">
    <div class="carousel-container">
        <div class="carousel-slide active" style="background-image: linear-gradient(135deg, #0b0a0b 0%, #656565 100%);">
            <div class="slide-overlay"></div>
            <div class="slide-content">
                <h1 class="slide-title">Edita like a Pro</h1>
                <p class="slide-description">Con inMD, sin saber nada de markdown</p>
                <a href="https://inmd.inled.es" class="slide-cta">
                    Probar
                    <span>→</span>
                </a>
            </div>
        </div>

        <div class="carousel-slide" style="background-image: linear-gradient(135deg, #08edd5 0%, #5f1afa 100%);">
            <div class="slide-overlay"></div>
            <div class="slide-content">
                <h1 class="slide-title">¿No tienes Word?</h1>
                <p class="slide-description">No hace falta que lo instales. Prueba inDOC</p>
                <a href="https://indoc.inled.es" class="slide-cta">
                    Explorar
                    <span>→</span>
                </a>
            </div>
        </div>

        <div class="carousel-slide" style="background-image: linear-gradient(135deg, #57d4e1 0%, #1062b6 100%);">
            <div class="slide-overlay"></div>
            <div class="slide-content">
                <h1 class="slide-title">Transforma tu conversación con ChatGPT a PDF</h1>
                <p class="slide-description">Sencillo, rápido, al instante. Prueba MDPDF</p>
                <a href="https://mdpdf.inled.es" class="slide-cta">
                    Vamos a ello
                    <span>→</span>
                </a>
            </div>
        </div>

        <div class="carousel-slide" style="background-image: linear-gradient(135deg, #d4d5fc 0%, #262ffc 100%);">
            <div class="slide-overlay"></div>
            <div class="slide-content">
                <h1 class="slide-title">La mejor apariencia para tu post de LinkedIN</h1>
                <p class="slide-description">Prueba inLinked y formatea tu post de Linkedin</p>
                <a href="https://inlinked.inled.es" class="slide-cta">
                    Usar
                    <span>→</span>
                </a>
            </div>
        </div>

        <button class="carousel-arrow prev">‹</button>
        <button class="carousel-arrow next">›</button>

        <div class="carousel-nav">
            <button class="nav-dot active"></button>
            <button class="nav-dot"></button>
            <button class="nav-dot"></button>
            <button class="nav-dot"></button>
        </div>

        <div class="progress-bar active"></div>
    </div>
    </div>
  
     <script>
        class UltraModernCarousel {
            constructor() {
                this.currentSlide = 0;
                this.slides = document.querySelectorAll('.carousel-slide');
                this.dots = document.querySelectorAll('.nav-dot');
                this.prevBtn = document.querySelector('.carousel-arrow.prev');
                this.nextBtn = document.querySelector('.carousel-arrow.next');
                this.progressBar = document.querySelector('.progress-bar');
                this.autoPlayInterval = null;
                this.autoPlayDelay = 5000;

                this.init();
            }

            init() {
                this.addEventListeners();
                this.startAutoPlay();
            }

            addEventListeners() {
                this.prevBtn.addEventListener('click', () => this.prevSlide());
                this.nextBtn.addEventListener('click', () => this.nextSlide());

                this.dots.forEach((dot, index) => {
                    dot.addEventListener('click', () => this.goToSlide(index));
                });

                // Pausar autoplay en hover
                document.querySelector('.carousel-container').addEventListener('mouseenter', () => {
                    this.stopAutoPlay();
                });

                document.querySelector('.carousel-container').addEventListener('mouseleave', () => {
                    this.startAutoPlay();
                });

                // Soporte para teclado
                document.addEventListener('keydown', (e) => {
                    if (e.key === 'ArrowLeft') this.prevSlide();
                    if (e.key === 'ArrowRight') this.nextSlide();
                });

                // Soporte para gestos táctiles
                let startX = 0;
                let endX = 0;

                document.querySelector('.carousel-container').addEventListener('touchstart', (e) => {
                    startX = e.touches[0].clientX;
                });

                document.querySelector('.carousel-container').addEventListener('touchend', (e) => {
                    endX = e.changedTouches[0].clientX;
                    this.handleSwipe();
                });
            }

            handleSwipe() {
                const swipeThreshold = 50;
                const diff = startX - endX;

                if (Math.abs(diff) > swipeThreshold) {
                    if (diff > 0) {
                        this.nextSlide();
                    } else {
                        this.prevSlide();
                    }
                }
            }

            updateSlides() {
                this.slides.forEach((slide, index) => {
                    slide.classList.remove('active', 'prev', 'next');
                    
                    if (index === this.currentSlide) {
                        slide.classList.add('active');
                    } else if (index < this.currentSlide) {
                        slide.classList.add('prev');
                    } else {
                        slide.classList.add('next');
                    }
                });

                this.dots.forEach((dot, index) => {
                    dot.classList.toggle('active', index === this.currentSlide);
                });

                this.restartProgressBar();
            }

            nextSlide() {
                this.currentSlide = (this.currentSlide + 1) % this.slides.length;
                this.updateSlides();
            }

            prevSlide() {
                this.currentSlide = (this.currentSlide - 1 + this.slides.length) % this.slides.length;
                this.updateSlides();
            }

            goToSlide(index) {
                this.currentSlide = index;
                this.updateSlides();
            }

            startAutoPlay() {
                this.stopAutoPlay();
                this.restartProgressBar();
                this.autoPlayInterval = setInterval(() => {
                    this.nextSlide();
                }, this.autoPlayDelay);
            }

            stopAutoPlay() {
                if (this.autoPlayInterval) {
                    clearInterval(this.autoPlayInterval);
                    this.autoPlayInterval = null;
                }
                this.progressBar.classList.remove('active');
            }

            restartProgressBar() {
                this.progressBar.classList.remove('active');
                setTimeout(() => {
                    this.progressBar.classList.add('active');
                }, 50);
            }
        }

        // Inicializar el carrusel cuando el DOM esté listo
        document.addEventListener('DOMContentLoaded', () => {
            new UltraModernCarousel();
        });

        // Animación de entrada suave
        window.addEventListener('load', () => {
            document.body.style.opacity = '0';
            document.body.style.transition = 'opacity 0.5s ease';
            setTimeout(() => {
                document.body.style.opacity = '1';
            }, 100);
        });
    </script>

    
</body>

</Layout>